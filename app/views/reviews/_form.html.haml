= form_for(@review_reply) do |f|
  -if @review_reply.errors.any?
    %div#error_explanation
      %h2
        = pluralize(@review_reply.errors.count, "error")

      %ul
      - @review_reply.errors.full_messages.each do |msg|
        %li
          = msg

  %div.field
    %b
      = f.label :url
    %br
    = f.text_field :url, :size => 140
  %div.field
    %b
      = f.label :comment
    %br
    = f.text_area :comment, :rows => 10, :cols => 100
  %div.actions
    = f.submit
